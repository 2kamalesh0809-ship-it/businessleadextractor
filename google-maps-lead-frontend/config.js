// Deployment Configuration
const API_BASE_URL = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1'
    ? 'http://localhost:5000'
    : 'https://businessleadextractor-backend.onrender.com'; // REPLACE this with your actual Render/Railway backend URL after deployment

// You can adds other configuration variables here
